Eureka server serves as service registry where all microservices are registered and there instances are maintained.

@EnableEurekaServer: makes it Eureka service running on default port 8761

http://localhost:8761/